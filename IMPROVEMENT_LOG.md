# Improvement Loop Log

Automated product-improvement loop. Each iteration: implement → test → commit → push.
Baseline at start: 21/21 tests passing on branch `improve/app-hardening`.

## Roadmap

| # | Focus | Status |
|---|-------|--------|
| 1 | Security: auth-gate payment status endpoint + tests | ✅ |
| 2 | Security: timing-safe admin login, signup rate limits, param validation | ✅ |
| 3 | Feature: pagination for appointment listings *(replaced async-wrapper idea — Express 5 forwards async errors natively)* | ✅ |
| 4 | Feature: password reset flow | ✅ |
| 5 | Feature: in-app notifications | ✅ |
| 6 | Feature: reschedule appointment | ✅ |
| 7 | Feature: email verification on signup | ✅ |
| 8 | Feature: salon review replies | ✅ |
| 9 | Feature: CSV export of salon appointments | ✅ |
| 10 | Feature: promo codes | ✅ |
| 11 | Feature: salon photo gallery | ✅ |
| 12 | Feature: per-salon booking lead time & slot step config | ✅ |
| 13 | Feature: no-show status + staff today-schedule endpoint | ✅ |
| 14 | Feature: favorites pagination + isFavorite flag in browse | ✅ |
| 15 | Feature: public staff directory per salon | ✅ |
| 16 | Feature: admin platform stats + audit log | ✅ |
| 17 | Feature: deep health check + request-id logging | ✅ |
| 18 | Feature: notification emails on cancel/decline/no-show | ✅ |
| 19 | Frontend polish (React) | ✅ |
| 20 | Test sweep: favorites & services CRUD coverage | ✅ |
| 21 | Docs: README feature matrix + final verification | ⏳ |

## Completed

- **#1 Payment status endpoint locked down** — `GET /api/pay/:orderId` previously had no auth; anyone with an order id could read payment details and trigger gateway syncs. Now requires a stakeholder role (paying customer via `Payment.customerID`, involved salon via `Payment.salonId`, or admin), enforced in route middleware + `canAccessPayment` controller check that runs *before* any Cashfree call. 7 new tests (`tests/payment-status-auth.test.js`). Tests: 21 → 28.

- **#2 Auth & input hardening** — admin login now uses `crypto.timingSafeEqual` over SHA-256 hashes (no timing leak, both fields always compared); signup endpoints (`/api/user/signup`, `/api/business/signup`, legacy `/api/buisness/signup`) now share the strict login rate limiter; public `getAllActiveServicesBySalonId` validates `salonId` with a zod query schema.

- **#4 Pagination for appointment listings** — added shared `paginateQuery`/`buildMeta` helpers (`utils/pagination.js`: page/limit coerced + clamped to 1..50, defaults 1/10) and applied them to customer `/api/appointment/getAll`, salon `/sceduledAppointments`, and admin `/appointments/getall` via `findAndCountAll`. Fully backward compatible: no `page`/`limit` params → legacy bare array (existing React + legacy JS consumers untouched); either param present → `{ data, page, limit, total, totalPages }` envelope. 15 new tests in `tests/pagination.test.js` cover clamping, both response shapes, meta math, and scoping. Tests: 28 → 43.

- **#5 Password reset flow** — customers can self-service forgotten passwords: `POST /api/user/forgot-password` issues a random 256-bit token of which only the SHA-256 hash plus a 60-minute expiry are stored (the plaintext link is emailed via a new `sendPasswordResetEmail` helper in the Brevo service, which stays a safe no-op when unconfigured), and `POST /api/user/reset-password` verifies token + email against the hash and window, rotates the bcrypt password (cost 10), and clears both token fields; both endpoints respond identically whether or not the account exists (no enumeration). They're public but share the strict login rate limiter — extracted to `middlewares/rateLimiters.js` so app.js mounts and route-level guards use one policy — with new zod schemas (`forgotPasswordSchema`, `resetPasswordSchema`, newPassword min 8). Because `sync()` never ALTERs existing tables, the two nullable user columns are backfilled at boot by a reusable `utils/ensureColumns.js` helper. 7 new tests in `tests/password-reset.test.js`. Tests: 43 → 50.

- **#6 In-app notifications** — a `notifications` table (`recipientRole` customer|salon, plain integer `recipientId` refs — no FK associations, so no circular-import risk) plus a fire-and-forget `notify()` service (`services/notificationService.js`) that catches and logs every failure so it can never break an already-successful booking flow. Hooks are wired after each DB write succeeds: booking finalized → salon gets "New booking" (`services/paymentService.js`, which is where bookings actually materialize from successful payments); status transitions confirmed/declined/completed → customer; customer cancellation → the other party (the salon). Three new endpoints at `/api/notifications` — list (newest-first, same backward-compatible legacy-array/envelope pagination contract as the appointment listings), mark-one-read (404s foreign/unknown ids), and read-all — scoped via `authMiddleware.requireRole('customer','salon','admin')` with customers keyed on `req.user.userId` and salons on `req.user.salonId`. 12 new tests in `tests/notifications.test.js`. Tests: 50 → 62.

- **#7 Reschedule appointment** — customers can move their own upcoming booking via `PATCH /api/appointment/:appointmentId/reschedule`: a pure `canReschedule(appointment, now)` rule (`utils/statusRules.js`) reuses the cancellation semantics (status pending|confirmed only, start strictly >24h away), ownership is enforced like cancel, and an optional `staffId` may reassign to any staff of the same salon who provides the same service. The new slot is revalidated against salon working hours and conflict-checked via `conflictingStaffIds` (blockouts + other bookings, now accepting an `excludeAppointmentId` so a booking can't collide with itself), returning 409 `New slot is not available` when busy; on success date/time/endTime/staff are persisted and the salon receives a fire-and-forget `booking.rescheduled` notification carrying old→new date/time. 7 new direct-controller tests in `tests/reschedule.test.js`. Tests: 62 → 69.

- **#8 Email verification** — customer signups now get a SOFT verification flow: when Brevo is configured (`emailService.isConfigured()` reads credentials without building the SDK client), signup issues a random 256-bit token whose SHA-256 hash plus a 24-hour expiry are stored on three new nullable user columns (`emailVerified`, `verificationTokenHash`, `verificationExpiresAt`, backfilled at boot by the existing `ensureColumns` call) and emailed fire-and-forget via a new `sendVerificationEmail` helper; unconfigured mailer → no token, account still created, nothing crashes. `POST /api/user/verify-email` flips the flag and burns the token on success (unknown/wrong/expired all get one generic 400) and `POST /api/user/resend-verification` always answers a generic 200 while rotating the token for known emails — both public but behind the strict rate limiter with new zod schemas. Accounts remain fully usable while unverified (flag exposed in signup response + profile for future gating; login responses untouched since they carry no user fields). 7 new tests in `tests/email-verification.test.js`. Tests: 69 → 76.

- **#9 Salon review replies** — salon owners can publicly answer customer reviews via `PUT /api/appointment/:appointmentId/reply` (role-gated to `salon`, zod `reviewReplySchema`: required reply, 1–1000 chars). The controller enforces salon ownership (403 for a different salon), 404s unknown appointments, and refuses to reply before the customer has actually reviewed (400 when both `rating` and `userReview` are null); replying is an upsert so owners can edit. Backed by a nullable `salonReply` TEXT column on Appointments (backfilled at boot by a new `ensureColumns(Appointment, ...)` call — TEXT works on SQLite and Postgres) that surfaces automatically in whole-row reads and was added to the explicit attribute list of the public `getSalonProfile` reviews listing. 6 new tests in `tests/review-replies.test.js`. Tests: 76 → 82.

- **#10 CSV export** — salon owners can download their full booking ledger via `GET /api/appointment/export/csv` (`authMiddleware` + `requireRole('salon')`, zod `csvExportSchema` validated against the query with the existing `validate(schema, 'query')` middleware; declared as a static path before any parameterized appointment routes). New pure, dependency-free helper `toCsv(rows, headers)` (`utils/csv.js`) emits RFC-4180-compliant output: fields containing commas/quotes/newlines get wrapped and embedded quotes doubled, every line CRLF-terminated. The controller scopes strictly to `req.user.salonId`, accepts optional `from`/`to` bounds (YYYY-MM-DD each, both optional, `from <= to` enforced by schema → 400 otherwise), queries ALL matching rows with no pagination cap ordered date ASC then time ASC, reusing the scheduled-appointments listing's include pattern for service/staff/customer names. Responds `text/csv; charset=utf-8` + `Content-Disposition: attachment; filename="appointments-<salonId>-<YYYYMMDD>.csv"`. Columns: AppointmentID, Date, Time, EndTime, Status, Service, Staff, Customer — deliberately **no Amount column**: Appointments store no price, and neither services (mutable) nor payments (null `orderId` on legacy rows) yield a reliably historical amount on these rows. 8 new tests in `tests/csv-export.test.js` (toCsv escaping/CRLF units; content-type+filename+header row; two-salon scoping; date-range filtering; from>to & malformed-date 400s). Tests: 82 → 90.

- **#11 Promo codes** — end-to-end discount codes. New `PromoCodes` table (`models/promoCodeModel.js`): unique `code` stored UPPERCASE via a model setter (one canonical form for lookups + uniqueness), `discountType` percent|flat, `discountValue` >0, nullable `maxDiscountAmount` (cap for the percent branch), `minOrderAmount` (default 0), nullable `validFrom`/`validUntil` window (inclusive), nullable `usageLimit` (null = unlimited) vs `usedCount`, `isActive`, and nullable `salonId` (null = redeemable at any salon). Booking flow: `paymentCreateSchema` gains an optional `promoCode` (trimmed + uppercased by zod); in `processPayment` the code is resolved **after** the authoritative price lookup via new `resolvePromo(code, salonId, amount)` (`services/paymentService.js`, alongside pure `computeDiscount` + `round2` helpers) — checks exist&active → window → salon scope → min order → usage limit, returning reason strings that map verbatim to 400s (`Invalid promo code`, `Promo code expired`, `Minimum order amount not met`, `Promo code usage limit reached`; wrong-salon codes deliberately report as *invalid* so callers can't probe other salons' codes without hitting the gateway — no axios mocking). Redemption accounting: three nullable Payment columns (`originalAmount`, `discountAmount`, `promoCodeApplied`) added to the model and backfilled at boot via a new `ensureColumns(Payment, 'payments', …)` call; the Cashfree order amount AND `Payment.orderAmount` both carry the discounted final charge (`max(0, round2(original − discount))`), keeping gateway and ledger consistent. On successful finalize, `finalizeAppointmentFromPayment` atomically increments the promo's `usedCount` exactly once per booking — the idempotent early-return means redirect+webhook replays can't double-count, and increment failures are swallowed so they never undo a captured booking. Salon management under `/api/salonsdashboard` (auth + `requireRole('salon')`): `POST /promos` (zod `promoCreateSchema`: code 3–20 chars `[A-Z0-9-_]` uppercased, percent ≤100, `validFrom ≤ validUntil`, positive coerced numbers; duplicate → 409), `GET /promos` (own only, inactive included as history), `PATCH /promos/:id` (own only, 403/404 otherwise; toggles `isActive` + edits cap/min/window/limit; `code`, `discountType`, `usedCount`, `salonId` immutable — stripped at schema level), `DELETE /promos/:id` is a **soft delete** (sets `isActive=false`, row kept so historical payments referencing the code keep a coherent audit trail). 24 new tests in `tests/promo-codes.test.js`: computeDiscount units (percent/flat/cap/rounding), every resolvePromo rule incl. min-order boundary equality, finalize usedCount increment + idempotency, endpoint scoping/toggle/soft-delete/403/404, and schema validation. Tests: 90 → 114.

- **#12 Salon photo gallery** — salons can curate a photo gallery of up to 10 image URLs. New nullable `galleryImages` TEXT column on Salons (stores `JSON.stringify(string[])` or null; backfilled at boot via `ensureColumns(Salons, 'salons', …)` — TEXT is valid on both SQLite and Postgres). Upsert via `PUT /api/salonsdashboard/gallery` (`authMiddleware` + `requireRole('salon')`, zod `gallerySchema`: 0–10 items, each trimmed and pinned to http(s) by regex rather than zod's scheme-agnostic `.url()`, duplicates collapse silently in a schema transform so a repeated paste doesn't 400; empty array stores NULL to clear). Ownership is token-scoped like every other salonsDashboard controller — no `:id` param exists to point at another salon, and writes always target `req.user.salonId`. A never-throwing `parseGallery(raw)` helper (`controllers/salonGalleryController.js`) degrades null/malformed JSON/non-array/non-string entries to a filtered array or `[]`; public reads (`getsalonbyId`, `getSalonProfile`, browse `getall`) now expose the parsed list as an `images` field via a shared `attachGallery()` that also strips the raw JSON blob from responses, so corrupt DB values can never turn a profile read into a 500. 11 new tests in `tests/salon-gallery.test.js`: parseGallery units, >10-URL / non-http(s) / empty-string schema rejections (middleware invoked directly), trim+dedupe pass-through, owner upsert + read-back on profile and detail endpoints, empty-list clear, garbage DB value → [], and cross-salon write isolation. Tests: 114 → 125.

- **#13 Booking lead time & slot step** — salons can now require advance notice and publish a time-grid. Two nullable INTEGER columns on Salons (`bookingLeadTimeMinutes`: null/0 = bookable immediately, max 1 week; `slotStepMinutes`: null = default 30) backfilled at boot via the existing `ensureColumns(Salons, 'salons', …)` call. Enforcement lives in one new helper — `validateLeadTime(salon, dateSelect, time, now)` in `services/availabilityService.js`, returning `{ ok, reason }` with the message `This salon requires bookings at least N minutes in advance` — and is applied next to `validateSalonHours` on **every** path where a booking happens: the availability checker `POST /api/appointment/check`, `processPayment` (so skipping the checker can't bypass it), and reschedule (the moved slot is a new booking). There is no server-side slot *generation* anywhere (clients submit times directly), so instead of generating grids the checker's response exposes the configured step informationally as `slotStepMinutes` via a new `{ availableStaff, slotStepMinutes }` envelope (both frontends updated to read `availableStaff`; `resolveSlotStepMinutes` + exported `DEFAULT_SLOT_STEP_MINUTES = 30` centralize the fallback). Salon management under `/api/salonsdashboard`: `GET/PUT /booking-config` (`authMiddleware` + `requireRole('salon')`, zod `bookingConfigSchema`: lead 0–10080 minutes (7 days), slotStep enum [10,15,20,30,45,60] or null), token-scoped to the calling salon like every dashboard controller. 11 new tests in `tests/booking-config.test.js`: too-soon payment-path block + beyond-threshold pass (Cashfree `createOrder` stubbed before require — the controller destructures at load time), exact-threshold boundary, null-lead bookable immediately, slot-step fallback matrix, checker payload exposure, config round-trip + cross-salon isolation, and schema rejections (middleware invoked directly). Tests: 125 → 136.

- **#14 No-show status + staff today schedule** — salons can mark a skipped booking as `no-show`, a new **terminal** status in `utils/statusRules.js`: the only path into it is `confirmed → no-show` (a pending booking must be confirmed or declined first — a no-show verdict on an unaccepted booking is meaningless), and nothing may follow it. The Appointment model's `isIn` validator gained the value so saves pass. Enforcement lives in `updateAppointmentStatus`: after the existing salon/staff ownership checks, `no-show` requires `req.user.role === 'salon'` (403 for staff and customers, even on their own appointments) and requires the appointment's start — parsed from the DATEONLY+TIME fields exactly like cancel/reschedule (`new Date(\`${date}T${time}\`)`) — to already be behind us (400 `Cannot mark a future appointment as no-show`). On success the customer receives a fire-and-forget `booking.no-show` notification via the existing `STATUS_NOTIFICATIONS` map (title "Booking marked no-show"). New `GET /api/staff/today` (`authMiddleware.requireRole('staff')`, declared as a static path before any parameterized route): returns the authenticated staff member's TODAY appointments (`where { staffId: req.user.staffId, date }` with the local calendar day computed from local getters and compared against the DATEONLY string), ordered `time ASC`, including service + customer names via the same include pattern as the legacy listing; bare-array response for drop-in frontend consumption. 8 new tests in `tests/noshow-and-staff-schedule.test.js`: canTransition units (confirmed→no-show true, pending→no-show false, terminality matrix), salon marks past confirmed (200 + notification row asserted), future appointment 400 with status untouched, pending-past 400, customer/staff role 403s, and /today scoping (yesterday/tomorrow rows excluded, a second staff member's same-salon same-day row excluded, out-of-order inserts proving time ASC). Tests: 136 → 144.

- **#15 Favorites enhancements** — favorites list + salon browse personalization. `GET /api/user/favorites` now supports the same backward-compatible pagination contract as the appointment listings (shared `paginateQuery`/`buildMeta`): no `page`/`limit` → legacy bare array of salon objects; either param → `{ data, page, limit, total, totalPages }`, both shapes newest-first by favorite `createdAt`. One gotcha found by tests: `findAndCountAll({ distinct: true })` is **wrong** on the composite-PK favorites table — Sequelize counts `DISTINCT userId`, collapsing a customer's whole list to total=1; the many-to-one salon include can't multiply rows so plain COUNT(*) is exact (documented in-code). Browse (`GET /api/business/getall`, plus the `/buisness` alias) gains optional auth: new `authMiddleware.optional` sets `req.user` when a valid Bearer token is present but silently proceeds anonymously on missing/stale tokens, so the public route never 403s. In `getAllSalons`, an authenticated **customer** gets boolean `isFavorite` attached to each returned salon via ONE batched `IN` query over just that page's salonIds (no N+1); anonymous visitors and non-customer roles get NO `isFavorite` field at all — absent, not false — keeping public payloads byte-identical to the pre-feature shape. 11 new tests in `tests/favorites-enhancements.test.js`: legacy vs envelope shapes, meta math incl. past-the-end page, newest-first ordering, per-customer scoping, true/false mix across a page for a 1-of-2 customer, anonymous + salon-role payload shape, and an N+1 proof that counts SQL touching `favorites` during a full browse (exactly 1). Tests: 144 → 155.

- **#16 Public staff directory** — customers can see who works at a salon and what each team member can book. New fully public `GET /api/business/staff?salonId=N` (+ `/buisness` alias) in `routes/buisnessRoutes.js`, placed with the other public reads — a static path, so no ordering/collision concerns; query-param style matching `getsalonbyId`, validated by new zod `staffDirectorySchema` (`salonId` coerced positive int → 400 pre-DB on garbage). Deliberately no `authMiddleware.optional` (unlike browse): the response carries no personalization, so an optional token would be dead weight. Controller `getSalonStaff` (`controllers/salonController.js`): 404 for unknown salons, but a known salon with nobody active is a valid empty roster (`200 []`). Returns clean explicitly mapped JSON — `[{ id, name, services: [{ id, name, price, duration, category }] }]` — active staff only (`statusbar='active'`) ordered by name ASC, each member's services scoped to their StaffServices pairing via one include (`through: { attributes: [] }`, service list ordered by name), so another salon's services can never leak. Contact details and credentials are stripped at both levels (email/phone/password/statusbar never leave the server). 7 new tests in `tests/staff-directory.test.js`: schema 400s incl. string coercion, active-only roster of the right salon ordered by name ASC, inactive-staff exclusion despite pairings, pairing scoping both directions (B's service absent from A's payload and vice versa), exact payload key sets with sensitive keys asserted absent, unknown-salon 404, empty-roster 200 []. Tests: 155 → 162.

- **#17 Admin stats + audit log** — platform-wide visibility for admins. New `GET /api/admin/stats` (`adminOnly` guard, same pattern as the rest of `routes/adminRoutes.js`): totalSalons/totalCustomers/totalStaff via three concurrent COUNTs, appointment status breakdown computed in ONE grouped query (`findAll({ attributes: [status, COUNT], group })`) mapped onto a fixed six-key `{ pending, confirmed, cancelled, completed, declined, 'no-show' }` shape that always serializes even at zero, revenue as a single `COALESCE(SUM(orderAmount), 0)` over `paymentStatus='Success'` only (the exact gateway/webhook success string — 'Pending' and 'Slot taken' rows are excluded), trailing-7-day new-customer signups via `createdAt >= now-7d`, plus a `serverTimestamp` ISO stamp. All aggregation through Sequelize fn/col/literal — no raw SQL interpolation. New append-only audit trail: `AdminAudit` table (`models/adminAuditModel.js`: adminEmail, action STRING(64), targetType STRING(32), targetId STRING(64), nullable details VARCHAR(255), createdAt only — no updatedAt), written fire-and-forget AFTER successful deletions via `recordAudit()` (`services/adminAuditService.js`), which mirrors the notificationService contract: never throws into the caller's flow, slices every field to its column width so oversized payloads can't reject the insert. Wired into `deleteUser` (action `user.delete`, details `name <email>`) and `deleteAppointment` (action `appointment.delete`, details enriched with customer name/email + salon name + slot via findByPk includes used purely for audit context). New `GET /api/admin/audit` reuses the shared pagination helpers with the same backward-compatible contract as every listing (no params → legacy bare array; either param → `{ data, page, limit, total, totalPages }` envelope), newest-first in both shapes with id as deterministic tiebreak. 10 new tests in `tests/admin-stats-audit.test.js`: full stats payload across fixtures incl. grouped statuses and success-only revenue sum, fresh serverTimestamp, requireRole('admin') 403/pass-through at controller level, route-file wiring asserted structurally (auth → roleGuard → controller on both paths), delete-user + delete-appointment audit rows (action/target/details identity, polled like the fire-and-forget notifications tests), recordAudit truncation units, and audit listing ordering + envelope meta math. Tests: 162 → 172.

- **#18 Deep health check + request-id logging** — every request is now traceable end-to-end. New `utils/observability.js`: `makeRequestId()` (crypto.randomUUID), `requestIdMiddleware` (sets `req.id`, reusing a well-formed incoming `X-Request-Id` header for gateway trace continuity while replacing anything malformed with a fresh UUID, and echoing the id back on every response via `X-Request-Id`), and `requestLogger` (exactly ONE access-log line per request on the response `finish` event — method, originalUrl, statusCode, durationMs, requestId — with every step wrapped in try/catch so logging can never break a request; deliberately mounted first, right after `trust proxy`, so even helmet/CORS/429-rejected requests are logged and carry the header). Lightweight `/health` untouched for Render's probe; new async `deepHealth()` pings the DB via `sequelize.authenticate()` try/catch and reports `{ ok, db: 'up'|'down', uptimeSeconds, memoryRssMb, nodeVersion, timestamp }` where `ok` mirrors DB status only — `GET /health/deep` maps that to 200 `{status:'ok', …}` vs 503 `{status:'degraded', …}`. Global error handler now prefixes its console.error line with `[req <id>]` so a client-reported X-Request-Id pins straight to the server-side stack. rawBody verify hook and CORS/rate-limit ordering untouched (middleware touches no body stream). 9 new tests in `tests/observability.test.js`: UUID format + uniqueness, valid-id reuse / malformed-id rejection across hostile values + header echo, one-line-on-finish with all five fields asserted, never-throws under a throwing res.on, deepHealth up-shape, and graceful db-down shape after a real `sequelize.close()`. Tests: 172 → 181.

- **#19 Status-change emails** — every booking status change now reaches the customer by EMAIL as well as in-app. New `sendBookingStatusEmail(toEmail, { status, salonName, serviceName, date, time })` in `services/emailService.js`: friendly per-status subject/body for confirmed/declined/completed/no-show/cancelled ("Your booking at X was cancelled" etc.), same fire-and-forget contract as the other helpers — `{ sent: false, reason: 'not-configured' }` when Brevo env is unset (checked lazily, so a key added later needs no restart), `missing-data` for unknown statuses/absent recipient, try/catch inside so it can never throw. The pure copy table is exposed via `bookingStatusSubject(status, salonName)` so subjects are assertable without sending mail. Wired into `controllers/appointmentController.js` via a private `sendCustomerStatusEmail(appointmentId, status)` helper that loads the customer's email + salon/service names in ONE `findByPk` include query and swallows every failure; called fire-and-forget (never awaited, `.catch(() => {})` belt-and-braces) after the DB write succeeds in `updateAppointmentStatus` (all successful transitions) and `cancelAppointment` — mirroring the existing `notify()` hooks, so a mail failure can neither delay nor break an already-successful response. 4 new tests in `tests/status-emails.test.js`: unconfigured no-op result for each mapped status without throwing, distinct non-empty subjects mentioning the salon (+ null copy for unknown status), and both happy paths (salon declines pending; customer cancels >24h out) still returning 200 with their notification rows written while the mailer is unconfigured. Tests: 181 → 185.

- **#20 Frontend: notifications UI + SPA rebuild** — the #6/#18 notification backend finally has a face. New reusable `<NotificationsPanel />` (`frontend/src/components/NotificationsPanel.jsx`) consumed by BOTH dashboards: mounted as a "Notifications" sidebar tab in SalonDashboard (Bell icon + live unread count in the nav label) and as a collapsible "Alerts" card section above the browse grid in CustomerDashboard (bell toggle in the header; panel stays mounted behind a display toggle so the badge is accurate before first open — matching the page's existing eager-fetch convention). Panel fetches `GET /api/notifications` on mount handling both response shapes (legacy bare array + `{ data }` envelope), maps type → tone-tinted icon chip + badge label for all seven server types (`booking.new|confirmed|declined|completed|cancelled|no-show|rescheduled`) with a generic bell fallback so future types can't crash it, shows title/body/relative time/unread dot, and offers per-item "Mark read" (`PATCH /:id/read`, optimistic local readAt stamp) plus a disabled-when-nothing-unread "Mark all read" (`POST /read-all`). Unread total flows up to parents via an optional `onUnreadChange(count)` callback. Styling stays inside the existing system — reuses `.booking-panel`/`.panel-title`/`.badge-*` classes plus a new `.notifications-*` block appended to `index.css` using only tokens.css vars (light+dark safe). `npm run build` passes clean (vite v8.1.5, no pre-existing errors); dist regenerated — new hashes `index-BdVtUVoF.js` / `index-D-Xg22hs.css` and a dedicated `NotificationsPanel` chunk.

- **#21 Test sweep** — closed the two biggest coverage holes without changing any behavior: full CRUD contracts for the customer favorites endpoints and the salon services console, both via direct-controller invocation in the established mock req/res style. `tests/favorites-crud.test.js` (7 tests): add → 200 + row persisted; duplicate add is idempotent (`findOrCreate` — re-adding is deliberately 200, never an error, still exactly 1 row); missing `salonId` → 400 with the table untouched; legacy bare-array listing returns full salon objects (no favorite join noise) newest-first; per-user scoping proven both directions; remove own pairing → 200 + row gone; removing an already-removed or never-added pairing → **404 `Favorite not found`** (current contract: destroy affecting 0 rows is reported as 404, asserted and commented). `tests/services-crud.test.js` (15 tests): owner creates a service (201) with `salonId` taken from the token rather than the body and `statusbar` defaulting to active; category defaults to "Other" when omitted; controller's own truthiness guard 400s on missing name/price/duration (with a comment that the zod schema is the stricter gate — tested directly at the middleware layer like the gallery suite: negative/zero/non-numeric price rejected, duration must be integer minutes 1..600 with the 600 boundary allowed, every documented category accepted, `statusbar` limited to active/inactive); listing scoped strictly to the calling salon across two salons; partial update persists price/duration/category while untouched fields stay put; soft deactivation via `statusbar=inactive` keeps the row; cross-salon update AND delete attempts both 403 with the row provably unchanged afterward; unknown ids 404 on update and delete. No production code touched — all current behavior matched existing docs/comments, no bugs surfaced. Tests: 185 → 207.

